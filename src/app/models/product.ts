export interface Product {
  name: string;
  description: string;
  image: string;
  user: string;
  price: number;
  id: string;
}

export interface createdProduct {
  name: string;
  description: string;
  image: string;
  user: string;
  price: number;
}
